import Vue from 'vue';

/**
 * 查找子组件
 * @param {String} name 组件名
 */
export function findChildComponent(target: Vue, name: string) {}

/**
 * 查找兄弟组件
 * @param {String} name 组件名
 */
export function findBrotherComponent(target: Vue, name: string) {}
